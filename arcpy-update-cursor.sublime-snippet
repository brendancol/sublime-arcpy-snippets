<snippet>
	<content><![CDATA[
with arcpy.da.UpdateCursor(targetFeatureClass, field_names=[], where_clause=None) as cursor:
    for r in cursor:
        break
        cursor.updateRow(r)
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>updateCursor</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.python</scope>
</snippet>
